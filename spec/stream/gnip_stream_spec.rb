require '../spec_helper'

describe GnipStream do

  let(:stream)  { GnipStream.new(ThinConnector::Environment.gnip_url) }
  it 'should reconnect after being inturupted' do

  end

  it 'should start the stream and execute the block passed in' do

  end
end